# P3-06UI Visual QA Notes

日期：2026-07-01

## 已确认

- 本地预览 `http://127.0.0.1:5178/#overview` 可打开。
- 登录页截图已保存为 `overview_initial.png`。
- 通过 Computer Use 切入开发演示态后，浏览器可访问树确认侧边栏已渲染为 7 个工作域：
  - 总览
  - 工作台
  - 客户
  - 知识运营
  - 质量复盘
  - 渠道接入
  - 管理运维
- 子入口保留：多渠道对话台、会话收件箱、人工审核、待发送草稿、工单/SLA、联系人中心、线索跟进、知识库运营、知识缺口闭环、知识评测、质量诊断、渠道健康、运维与告警、模型路由、系统设置。
- P3-06UI 第二片使用 Playwright mock `/api/auth/me` 返回 owner 演示用户，点击“开发演示进入”后确认“管理员视图”和 7 个工作域可见。
- P3-06UI 第二片截图已保存为 `p3_06ui_role_navigation_owner_overview.png`。
- P3-06UI 第三片使用 Playwright 从 `#ops` 进入管理运维，确认 `运维与告警`、`模型路由`、`系统设置` 三个内部 Tab 均可切换。
- P3-06UI 第三片截图已保存为 `p3_06ui_admin_ops_tabs.png`。
- P3-06G 指标出口第一片使用 Playwright 从 `#ops` 进入演示态，确认 `指标出口`、`采集文本预览` 和 `wanfa_worker_stale` 可见。
- P3-06G 截图已保存为 `p3_06g_ops_metrics.png`。

## 截图说明

早期 macOS `screencapture` 曾被 Chrome 另一个播放中的标签页抢占画面，因此没有保留那次 post-login 截图。2026-07-01 第二片和第三片已改用 Playwright 直接访问本地预览并保存有效截图。

## 观察结论

导航收束后首屏理解成本下降明显。桌面侧边栏已改为独立滚动，避免两级导航在不同屏幕高度下压住底部工作域；第二片已经完成前端角色化默认入口和菜单可见性；第三片已经完成管理运维内部二级 Tab；P3-06G 已补指标出口第一片。下一步优先进入 RBAC 收口第一片。
