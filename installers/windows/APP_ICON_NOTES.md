# Windows 图标候选说明

当前阶段只固定图标规范和候选位置，不生成正式 `.ico` 或签名 `.exe`。

## 候选规范

- 名称：万法常世客服中台。
- 最终文件建议：`installers/windows/assets/AppIcon.ico`。
- 后续若生成快捷方式或 `.exe` 包装器，再绑定正式 `.ico`。

## 边界

- 当前 `.bat` 与 PowerShell 入口仍是候选包装。
- 不是正式签名 Windows 安装包。
- 图标文件不得包含客户商标、客户数据或平台图标授权风险素材。
